import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const SuscribeModal = () => {
  return (
    <View>
      <Text>SuscribeModal</Text>
    </View>
  );
};

export default SuscribeModal;

const styles = StyleSheet.create({});
